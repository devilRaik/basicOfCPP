#include<iostream> // Header file which provide Input/Output functionality with std(standerd) namespace which is includ before cout
// using namespace std;
void main(){ 
   std::cout<<"Hello world"; // "::" scope resolution operator
}

// Low level language - Nearest to machine and it's except only 0 or 1 means it is applyed with layers of abstraction/Word convert into series of 0 or 1 and then it's convert into high level langauge